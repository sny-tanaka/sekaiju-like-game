/**
 * ここにこの関数の説明を書く
 * @param fuga ここにfugaの説明を書く
 * @returns ここに戻り値の説明を書く
 */
export const {{camelCase name}} = ({ fuga }: Param): Result => {
  return {};
};

type Param = {
  fuga: string;
};

type Result = {};
