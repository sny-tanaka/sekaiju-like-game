import { {{camelCase name}} } from './{{camelCase name}}';

describe('{{camelCase name}}', () => {
  test('ここにテストの説明を書く', () => {
    const result = {{camelCase name}}({ fuga: 'fuga' });
    expect(result).toEqual({});
  });
});
